# Xcode / Swift
.DS_Store
xcuserdata/
*.xcuserstate
DerivedData/
build/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# XcodeGen output — regenerate with `xcodegen generate`
Seret.xcodeproj/

# Swift Package Manager
.build/
.swiftpm/
Packages/*/.build/

# Secrets — copy Secrets.example.xcconfig → Secrets.xcconfig and fill in
Secrets.xcconfig
*.secret.xcconfig

# CocoaPods (only if VLCKit ends up via Pods)
Pods/

# Vendored frameworks — TVVLCKit.xcframework is fetched by Scripts/fetch-frameworks.sh (Plan 7c)
Frameworks/

# Superpowers brainstorm scratch
.superpowers/

# XcodeGen-generated Info.plist (carries the $(TMDB_API_KEY) build-var ref; regenerated by `xcodegen generate`)
Apps/SeretTV/Info.plist

# Misc
*.log
